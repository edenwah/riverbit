interface DesktopNavRightProps {
    balance: string;
    points: string;
    language: string;
    setLanguage: (lang: string) => void;
}
export default function DesktopNavRight({ balance, points, language, setLanguage, }: DesktopNavRightProps): import("react/jsx-runtime").JSX.Element;
export {};
