import React from "react";
interface LeverageSliderProps {
    value: number;
    onChange: (val: number) => void;
}
declare const LeverageSlider: React.FC<LeverageSliderProps>;
export default LeverageSlider;
